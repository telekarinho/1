<?xml version="1.0" encoding="utf-8"?>
<!--
  WebPlayerActivity layout: WebView fullscreen + status overlay opcional.
  A tv.html web ja injeta seus proprios overlays (relogio, ticker, QR, emergencia),
  entao aqui so precisamos da WebView ocupando 100% da tela e um status pra
  mostrar mensagens enquanto resolve o code -> fid+tvId.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true">

    <WebView
        android:id="@+id/tvWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layerType="hardware" />

    <TextView
        android:id="@+id/statusView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text=""
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:fontFamily="sans-serif-medium"
        android:padding="24dp"
        android:visibility="gone"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6" />

</FrameLayout>
